<h1>Editing Book</h1>
<%= form_with model:@book, local: true do |f| %>
  <p4>Title</p4>
  <%= f.text_field :title %>
  <p4>Body</p4>
  <%= f.text_area :body%>
  <%= f.submit 'Update Book' %>
<% end %>